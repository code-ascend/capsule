image: registry.altlinux.org/sisyphus/base:latest
output: /var/cache/capsule
compression: zstd
install:
  - name: fix apt
    run: |
      echo 'RPM::Options:: "--ignoresize";' >> /etc/apt/apt.conf

  - name: deps
    run: |
      apt-get update
      apt-get install -y hello
